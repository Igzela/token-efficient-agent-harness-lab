# Test Fixtures

`stage0_events_with_line17_issue.jsonl` is copied from
`docs/stage0/events.jsonl` and intentionally preserves the known line 17
concatenated duplicate event issue.

Do not repair this fixture in place; tests rely on the malformed line.
