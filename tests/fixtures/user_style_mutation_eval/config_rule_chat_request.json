{
  "schema_version": "user_style_mutation.v1",
  "case_id": "config-rule-chat-001",
  "base_fixture_id": "config-rule-project",
  "variant_type": "user_style_chat_request",
  "user_prompt": "can you update the lint rules? we need to change the thresholds for the new standard. the config and a summary doc would be great",
  "expected_task_family": "config_rule_change",
  "expected_required_fields": ["task_id", "objective"],
  "expected_missing_fields": ["allowed_files", "forbidden_files", "required_files"],
  "admission_expectation": "needs_clarification",
  "evidence_refs": ["fixture:config-rule-project/task-spec", "mutation:chat-request-variant"],
  "fixture_metadata": {
    "fixture_id": "config-rule-chat-001",
    "source_type": "mutated_user_style",
    "freshness": "2026-05-19",
    "estimated_human_minutes": 3.0,
    "difficulty": "easy",
    "contamination_risk": "low",
    "admission_scope": "diagnostic"
  }
}
