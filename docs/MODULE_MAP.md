# Module Map

The legacy Python reference implementation (`src/harness_core/`) and its test suite (`tests/`) have been retired. The Rust `engine/` is now the sole runtime implementation. Python is retained only as the REST SDK (`sdk/python/`) and utility scripts.

## Reachability Classification

Batch 1 classified modules for the supervised autonomous beta planning track. Classes:

- **active** — wired into the primary Rust runtime/API/dashboard/SDK or primary verification path.
- **partial** — implemented and tested, but not wired into the primary runtime/API.
- **library-only** — exported/tested utility surface used by tests, scripts, or future adapters.
- **dormant** — retained unwired reference/parity code.
- **legacy-delete-candidate** — likely removable only after a later explicit deletion review; no deletion is approved here.

| Class | Modules |
| --- | --- |
| active | `engine/src/main.rs`, `engine/src/http_server/`, `engine/src/scheduler.rs`, `engine/src/read_only_planner.rs`, `engine/src/dispatch_engine.rs`, `engine/src/task_analyzer/`, `engine/src/model_selector.rs`, `engine/src/budget_manager.rs`, `engine/src/executor_adapter.rs`, `engine/src/dispatch_ledger.rs`, `engine/src/provider/`, `engine/src/cli/`, `engine/src/node_executor.rs`, `engine/src/storage/local_product_store/`, `engine/src/storage/backup_manager.rs`, `engine/src/infrastructure/auth.rs`, `engine/src/infrastructure/rate_limiter.rs`, `dashboard/`, `sdk/typescript/`, `sdk/python/`, `codegen/generate_wire_types.py`, `wire_contract/v1/`, primary smoke/ops/check/pilot scripts |
| partial | `engine/src/orchestration/`, `engine/src/workflow/dag_manager/`, `engine/src/workflow/concurrency/`, `engine/src/workflow/context_pack/`, `engine/src/workflow/checkpoint.rs`, `engine/src/workflow/dag_mutations.rs`, `engine/src/quality/`, `engine/src/routing/`, `engine/src/ecosystem/`, `engine/src/doc_generator.rs`, `engine/src/harness/` |
| reference-only | `engine/src/event_source/` (append-only event sourcing, incompatible with SQLite CRUD), `engine/src/event_schema.rs` (wire contract parity), `engine/src/errors.rs` (event store error types) |
| library-only | `engine/src/runtime.rs`, `engine/src/evaluation_stub.rs`, `engine/src/wire_types.rs`, `engine/src/harness/model_profiles/`, `tools/check_security_baseline.py`, dashboard/static and security test helpers |

Batch 2 model decision: `WorkflowGraph` is the canonical planning and persistence model, `DAGState` remains graph-mutation state for versioned proposals and rollback, and scheduling-local `DagState` remains a concurrency view. Batch 3 implements the read-only planner against `WorkflowGraph` and app-owned SQLite `workflow_plans` without execution authority. Batch 4 implements inert durable workflow run/node/edge/event/approval metadata in app-owned SQLite without execution authority. Batch 5 adds recommendation-only quality/routing/retry/observability advisory metadata to read-only plans without execution authority. Batch 6 is documentation/design only. Batch 7 Slice A adds storage-only supervised patch workspace/artifact metadata in `engine/src/storage/local_product_store/` without execution authority. Batch 7 Slice B adds GET-only HTTP visibility for that metadata, Slice C adds TypeScript/Python SDK read-only visibility, Slice D adds docs-only approval-binding design, and Slice E adds dashboard read-only supervised patch metadata visibility (read-only tab with list/detail drill-down, boundary badges; typecheck/lint/build verified; no Rust/API/storage changes), without create/update/delete or execution authority. No R8 or broad refactor is approved.

## Dynamic Workflow Readiness Notes

The current system has supervised workflow execution primitives, but not Claude Code-style dynamic workflows. Existing modules should be deepened rather than duplicated:

| Need | Existing modules to extend | Current gap |
| --- | --- | --- |
| Runtime graph mutation | `engine/src/workflow/dag_manager/`, `engine/src/storage/local_product_store/workflow_runs.rs`, `engine/src/scheduler.rs` | **DONE (Batch 1):** DAGManager→persistence bridge, mutation events (node_added/removed/status_updated, edge_added/removed/rewired), replay with completed-node protection, batch mutation, import/export round-trip. |
| Controller loop | `engine/src/scheduler.rs`, `engine/src/orchestration/workflow_engine.rs`, `engine/src/node_executor.rs`, `engine/src/workflow/dynamic_controller.rs` | **DONE (Batch 2):** DynamicWorkflowController owns observe→choose→tick→evaluate→mutate→pause/finish loop. Auto-fix on failure creates fix+test nodes. Quality review on completed nodes. Mutation limit with approval gate. 24 tests. |
| Adaptive routing | `engine/src/routing/`, `engine/src/scheduler.rs`, `engine/src/provider/`, `engine/src/storage/local_product_store/feedback.rs` | **DONE (Batch 3):** `scheduler_feedback` table persists per-node execution feedback (executor_type, task_group, success, latency_ms, retry_count, quality_score, cost, error_domain). `FeedbackRecord`/`FeedbackStoreStats` types. `suggest_executor_type` returns best executor from history. `tick_with_executor_and_command` auto-records feedback. `DynamicWorkflowController` exposes `suggested_executor_type`. 10 tests. |
| Dynamic decomposition | `engine/src/read_only_planner.rs`, `engine/src/orchestration/task_decomposer.rs`, `engine/src/task_analyzer/` | **DONE (Batch 4):** `Decomposer` trait with `ObservationDecomposer`, `TestFailureDecomposer`, `QualityFailureDecomposer`, `GoalDrivenDecomposer` implementations. DynamicWorkflowController integrates decomposers for observation/test/quality/goal-driven graph proposals. 46 new tests. |
| Agent profiles / subagent runs | `engine/src/orchestration/agent_role_registry.rs`, `engine/src/cli/`, `engine/src/node_executor.rs` | Nodes can run CLI executors, but reusable planner/implementer/reviewer/tester profiles with scoped tools/context are not first-class runtime records. |
| Tool registry and hooks | `engine/src/infrastructure/`, `engine/src/http_server/`, future metadata tables | No audited pre/post execution hook registry or MCP-like tool capability metadata is wired into workflow decisions. |

Minimum dynamic-workflow acceptance requires a persisted run to change its graph after observing results, then continue execution with replayable audit evidence. Running a fixed workflow graph through CLI executor is supervised execution, not dynamic workflow orchestration.

Adapter status after Batch 3:

| Source | Target | Purpose | Constraint |
| --- | --- | --- | --- |
| `DAGState` | `WorkflowGraph` | Convert approved graph-mutation snapshots into canonical planning records. | Test-first; no execution authority. |
| `WorkflowGraph` | `DagState` | Feed concurrency scheduling from canonical planning records. | Drop-only view; preserve node ids/status and edge dependencies. |
| `WorkflowGraph` | persisted app-owned SQLite rows | Store read-only planner state in Batch 3, inert workflow run state in Batch 4, and recommendation-only advisory metadata in Batch 5. Batch 6 adds only design-gate docs for future execution boundaries. Batch 7 Slice A adds separate app-owned supervised patch metadata tables, Slice B adds GET-only metadata visibility, Slice C adds SDK read-only visibility, Slice D adds approval-binding design only, and Slice E adds dashboard read-only visibility; none wire workflow graph execution. | App-owned state only; no target repo writes or execution authority. |

## Rust Engine Modules

| Module | Stage | Purpose | Main public APIs | Related tests |
| --- | --- | --- | --- | --- |
| `engine/src/runtime.rs` | Language Migration Phase 1 | Deterministic Rust fixture runtime for stable timestamps and IDs. | `FixtureRuntime`, `FIXTURE_TIMESTAMP` | `engine/tests/dispatch_parity.rs` |
| `engine/src/event_schema.rs` | Language Migration Phase 1 (reference-only) | Rust event.v1 validation, canonical JSON, and stable idempotency hash helpers. Retained for wire contract parity; not used by active runtime. | `validate_event`, `canonical_event_json`, `stable_idempotency_hash` | `engine/tests/dispatch_parity.rs` |
| `engine/src/task_analyzer/` | Language Migration Phase 1 + Architecture Refactor R3 | Rust rule-based task analyzer module directory. `mod.rs` owns `TaskAnalysis`, `RuleBasedTaskAnalyzer`, `analyze()`. `rules.rs` static keyword/phrase/multiplier maps. `classify.rs` domain/intent classification. `risk.rs` risk flag detection and negation. `scoring.rs` complexity, budgets, confidence, risk level, quality, escalation, capabilities, features, and read-only advisory risk downgrade detection. | `RuleBasedTaskAnalyzer`, `TaskAnalysis`, `TASK_ANALYSIS_SCHEMA_VERSION`, `analyze` | `engine/tests/dispatch_parity.rs` |
| `engine/src/dispatch_decision.rs` | Language Migration Phase 1 | Rust dispatch decision schema structs used by the parity engine. Split deferred; no further R-series file splitting is approved. | `DispatchDecision`, `BudgetReservation`, `ExecutionGate`, `build_dispatch_bundle` | `engine/tests/dispatch_parity.rs` |
| `engine/src/model_selector.rs` | Language Migration Phase 2 | Rust model-tier selector with static routing policy, risk escalation, fallback, shadow routes, and rejected candidates. | `DispatchRoutingPolicy`, `ModelSelector`, `ModelSelection` | `engine/tests/dispatch_parity.rs` |
| `engine/src/budget_manager.rs` | Language Migration Phase 2 | Rust pre-execution token/cost budget reservation manager. | `BudgetManager` | `engine/tests/dispatch_parity.rs` |
| `engine/src/executor_adapter.rs` | Language Migration Phase 2 | Rust executor abstraction with default noop executor; does not call providers. | `Executor`, `NoopExecutor`, `ExecutionResult` | `engine/tests/dispatch_parity.rs` |
| `engine/src/provider/` | Language Migration Phase 2 + Provider Infrastructure + Stage 1 + Productization Phase 1 | Rust provider module directory: boundary trait, config, credential, audit, redaction, transport, cost gate, retry (with RetryFallbackManager), provider pricing env parsing, and provider adapters (openai, anthropic, stub). | `Provider`, `DisabledProvider`, `ProviderRequest`, `ProviderResponse`, `ProviderError`, `ProviderConfig`, `ProviderPricingConfig`, `CredentialRef`, `RetryPolicy`, `CredentialBoundary`, `ProviderAuditEvent`, `ProviderAuditRecorder`, `redact_secrets`, `redact_audit_fields`, `RetryFallbackManager`, `should_retry`, `compute_delay_ms`, `CostGateConfig`, `check_cost_gates`, `CostGateBlock` | `cargo test -p engine` |
| `engine/src/evaluation_stub.rs` | Language Migration Phase 2 + Dormant Adaptation Phase 1 | Rust deterministic evaluation stub for noop execution results and human-review status. Phase 1 extracted `trait Evaluator` so `DispatchEngine` holds `Box<dyn Evaluator>`. | `EvaluationStub`, `EvaluationResult`, `EvaluationCheck`, `Evaluator` (trait) | `engine/tests/dispatch_parity.rs` |
| `engine/src/quality/evaluator_bridge.rs` | Dormant Adaptation Phase 3 | `QualityGateEvaluator` implements `Evaluator` trait, routing execution results through the quality module gate chain: `FinalGateRunner` → `ArtifactGate` → `ScoringEngine` → `QualityGateManager`. Opt-in replacement for `EvaluationStub` via `DispatchEngine::with_evaluator()`. | `QualityGateEvaluator` | `cargo test -p engine --lib quality::evaluator_bridge` (5 tests) |
| `engine/src/dispatch_ledger.rs` | Language Migration Phase 2 | Rust dispatch record and bundle ledger structs for audit-chain parity. | `DispatchLedger`, `DispatchRecord`, `DispatchBundle` | `engine/tests/dispatch_parity.rs` |
| `engine/src/dispatch_engine.rs` | Language Migration Phase 2 + Dormant Adaptation Phase 3 | Rust dispatch orchestrator that wires analyzer, selector, budget, executor, evaluator, and ledger into dispatch bundles, including provider execution gating and read-only advisory human-review warnings. Phase 3: `with_advisor()` integrates `AdvisorBroker` as optional pre-dispatch advisory layer enriching `execution_policy.advisory`. | `DispatchEngine`, `build_dispatch_bundle` | `engine/tests/dispatch_parity.rs`, `engine/tests/test_dispatch_engine.rs` |
| `engine/src/read_only_planner.rs` | Supervised Autonomous Beta Planning Batch 3-5 | Planning-only read-only planner. Builds canonical `WorkflowGraph` plans from `RuleBasedTaskAnalyzer`, `TaskDecomposer`, and `DependencyResolver`; emits explicit no-execution/no-target-write/no-worker boundaries plus Batch 5 recommendation-only advisory metadata from pure quality/routing/retry/observability inputs. Batch 6 did not change this module. | `ReadOnlyPlanner`, `WorkflowPlanIds`, `READ_ONLY_PLAN_SCHEMA_VERSION`, `PLAN_ADVISORY_SCHEMA_VERSION`, `read_only_boundaries` | `engine/src/read_only_planner.rs` (inline tests), `engine/tests/test_http_server.rs`, `engine/tests/test_local_product_store.rs`, `engine/tests/test_data_operations.rs` |
| `engine/src/http_server/` | Language Migration + Production-Grade Track | Rust HTTP server module directory. `handlers/` 15 modules (health, dispatch, plans, workflow_runs, supervised_patch, team, keys, costs, backups, audit, provider, dashboard, data_ops, operations, scheduler). `middleware.rs` adds CORS origin matching layer (`cors_layer`), health probe bypass, X-Request-ID, rate-limit. `routes.rs` wires `cors_layer` and `request_id_layer` middleware. Tick handler supports `command` override field. `WorkflowRunTickApiRequest` includes optional `command` for node_metadata injection. | `ServerContext`, `ServerConfig`, `AxumApiState`, `ApiError`, `build_axum_router`, `build_axum_router_with_dashboard`, `openapi_document` | `engine/tests/test_http_server.rs` (75 tests) |
| `engine/src/doc_generator.rs` | Language Migration Rust Engine/API Parity | Rust documentation generator with module/schema registry, Rust source parser, and markdown generation. | `DocGenerator`, `ModuleDoc`, `parse_module_from_source` | `engine/tests/test_doc_generator.rs` |
| `engine/src/storage/local_product_store/` | Agent-Control-Plane Local Small-Team Productization + Permission Governance + Cost Governance + Dashboard Controls + Architecture Refactor R2 + Supervised Autonomous Beta Planning Batch 3-5 + Batch 7 Slice A-B | SQLite-backed app-owned local state module directory. `mod.rs` owns `LocalProductStore` struct, constructors, shared helpers, DDL, and re-exports. Domain submodules: `dispatch.rs`, `plans.rs`, `workflow_runs.rs`, `supervised_patch.rs`, `config.rs`, `team.rs`, `keys.rs`, `audit.rs` with server-side audit search, `provider_audit.rs`, `costs.rs` with estimated-cost availability, `migrations.rs`, `integrity.rs`, `export_import.rs`, `boundaries.rs`. `plans.rs` persists app-owned non-executable `WorkflowGraph` plans plus Batch 5 advisory metadata in `workflow_plans`; `workflow_runs.rs` persists inert run/node/edge/event/approval metadata in `workflow_runs` child tables; `supervised_patch.rs` persists storage-only patch workspace/artifact metadata in app-owned SQLite with path-boundary validation. Slice B exposes these records through read-only HTTP GET routes and Slice C adds SDK read-only wrappers; no UI/runtime execution surface exists. | `LocalProductStore`, `WorkflowPlanIds`, `local_boundaries`, `IntegrityReport`, `ImportResult`, `ImportCounts`, supervised patch metadata methods | `engine/tests/test_http_server.rs`, `engine/tests/test_local_product_store.rs`, `engine/tests/test_data_operations.rs`, `engine/tests/test_audit_integrity.rs` |
| `engine/src/cli/` | CLI Executor Routing | Pre-existing local subprocess exception for complexity-based dispatch to Claude Code CLI / Codex CLI. `cli_node_executor.rs` bridges CLI executors to `NodeExecutor` trait for workflow tick. Gated behind `ACP_ENABLE_CLI_EXECUTION=1`. | `ClaudeCodeCliExecutor`, `CodexCliExecutor`, `CliNodeExecutor`, `MultiExecutor`, `CliConfig` | `cargo test -p engine` (12 CLI tests) |
| `engine/src/scheduler.rs` | Production-Grade Track Phase 2 + GA-3 + Dormant Adaptation Phase 2 + Phase 3 | Persistent workflow scheduler. `SchedulerConfig` with `from_env()` reading `ACP_SCHEDULER_*` vars. `WorkflowScheduler` with background thread loop, lease recovery, `ACP_SCHEDULER_EXECUTOR` selects executor type (noop/command/claude_code_cli/codex_cli). Status includes `active_runs` from live store. **Phase 2 activated**: `SchedulerModules` struct holds `WorkQueue`, `ResultAggregator`, and `FeedbackIntegrator` with `AutoDowngradePolicy`/`AutoUpgradePolicy`; scheduler tick uses WorkQueue for in-memory graph state tracking, records node outcomes for adaptive routing feedback via FeedbackIntegrator, and produces structured aggregation via ResultAggregator on terminal runs. **Phase 3 activated**: `ConflictResolver` + `HumanApprovalGate` + `WorkflowEngine` added to `SchedulerModules`; conflict detection/resolution and approval gate checks run before aggregation on terminal runs; `WorkflowEngine::tick()` provides DAG-aware processing for multi-node runs with edges. Status includes `dormant_modules_active` field with 7 active modules. | `SchedulerConfig`, `WorkflowScheduler` | `cargo test -p engine --lib scheduler` (30 tests) |
| `engine/src/node_executor.rs` | Supervised Autonomous Beta Planning Batch 7 Slice F | Node-level executor trait and implementations for workflow tick. `NodeExecutor` trait with `execute_node()`. `NoopNodeExecutor` (always succeeds), `StubNodeExecutor` (template output), `FailNodeExecutor` (always fails), `CommandNodeExecutor` (allowlisted binary exec with shell-metachar rejection, timeout kill, cwd=workspace, structured stdout/stderr/exit_code). No `sh -c` — argv parsed directly. | `NodeExecutor`, `NodeExecutionInput`, `NodeExecutionOutput`, `NoopNodeExecutor`, `StubNodeExecutor`, `FailNodeExecutor`, `CommandNodeExecutor` | `cargo test -p engine --lib node_executor` (8 tests) |
| `engine/src/workflow/dag_manager/` | Workflow DAG Management + Architecture Refactor R4 + Dormant Adaptation Phase 2 | DAG manager module directory. `mod.rs` owns `DAGManager` struct, mutation dispatch, rollback, validation, topological sort, ready-nodes, path finding, re-exports, and tests. **Phase 2 activated in `ReadOnlyPlanner`**: builds DAGManager from TaskDecomposer graph output, validates DAG structure, exposes topological order, supported mutations metadata, and DAGMutationLimits in plan output. `types.rs` DAGNode/DAGEdge/DAGState/DAGMutationProposal/DAGMutationResult. `helpers.rs` cycle detection (iterative DFS), node/edge lookup, approval gating. `mutations.rs` add/remove/rewire/update applicators. `compensate.rs` inverse mutation generator. | `DAGManager`, `DAGNode`, `DAGEdge`, `DAGState`, `DAGMutationProposal`, `DAGMutationResult`, `has_cycle`, `find_node`, `find_edge`, `requires_approval`, `apply_add_node`, `apply_remove_node`, `apply_add_edge`, `apply_remove_edge`, `apply_rewire_edge`, `apply_update_node`, `compensate` | `engine/src/workflow/dag_manager/mod.rs` (24 inline tests) |
| `engine/src/workflow/context_pack/` | Context Pack v2 + Architecture Refactor R5 + Dormant Adaptation Phase 2 | Context pack module directory. `mod.rs` re-exports, inline tests. `rules.rs` schema versions, allowed-value arrays, required-field arrays. `types.rs` ContextBudget, RetrievalPolicy, MemoryDigest, ContextLayers structs. **Phase 2 activated in `RuleBasedTaskAnalyzer`**: `TaskAnalysis.context_budget()` derives ContextBudget from analysis output; `TaskAnalysis.context_pack_validation()` runs `check_budget_compliance` and `validate_context_layers`. `validation.rs` five public validators (advisor pack, model pack, retrieval request, retrieval result, context layers) plus internal helpers. `budget.rs` budget compliance and prune policy logic. | `ContextBudget`, `RetrievalPolicy`, `MemoryDigest`, `ContextLayers`, `validate_advisor_context_pack_v2`, `validate_model_context_pack_v2`, `validate_context_retrieval_request`, `validate_context_retrieval_result`, `validate_context_layers`, `check_budget_compliance`, `apply_prune_policy` | `engine/src/workflow/context_pack/mod.rs` (17 inline tests) |
| `engine/src/workflow/concurrency/` | Workflow Concurrency Scheduling + Architecture Refactor R7 | Concurrency scheduling module directory. `mod.rs` re-exports, inline tests. `dag_types.rs` DagNode, DagEdge, DagState with Default impls (local subset for scheduling). `types.rs` FileOverlap, ScheduleBatch structs. `controller.rs` ConcurrencyController with schedule, detect_file_overlaps, can_run_parallel. `helpers.rs` item_id, metadata, read_files, write_files, conflicting_files, blocking_reason, edge_blocks. | `DagNode`, `DagEdge`, `DagState`, `FileOverlap`, `ScheduleBatch`, `ConcurrencyController`, `item_id`, `metadata`, `read_files`, `write_files`, `conflicting_files`, `blocking_reason`, `edge_blocks` | `engine/src/workflow/concurrency/mod.rs` (22 inline tests) |
| `engine/src/harness/model_profiles/` | Harness Model Profiles + Architecture Refactor R6 | Model profiles module directory. `mod.rs` re-exports, inline tests. `constants.rs` schema versions, enum sets (TIERS, TOOL_STRICTNESS, etc.), required-field arrays. `types.rs` CostMetadata, ForbiddenPreviousTool, ModelHarnessProfile, ShadowRoutingRecommendation structs. `validation.rs` validate_model_harness_profile, validate_shadow_routing_recommendation validators plus internal helpers (cost metadata, forbidden tools, tool conflict, credential detection). `shadow.rs` is_shadow_only, can_compare_with_usage_ledger. | `MODEL_PROFILE_SCHEMA_VERSION`, `SHADOW_ROUTING_SCHEMA_VERSION`, `TIERS`, `CostMetadata`, `ForbiddenPreviousTool`, `ModelHarnessProfile`, `ShadowRoutingRecommendation`, `validate_model_harness_profile`, `validate_shadow_routing_recommendation`, `is_shadow_only`, `can_compare_with_usage_ledger` | `engine/src/harness/model_profiles/mod.rs` (16 inline tests) |

## SDK and Dashboard

| Module | Stage | Purpose | Main public APIs | Related tests |
| --- | --- | --- | --- | --- |
| `sdk/typescript/` | Agent-Control-Plane Phase 5 + Local Small-Team Productization + Dashboard Controls + Post-R7 Wire/Type Governance + Production-like Local Ops Hardening + Supervised Autonomous Beta Planning Batch 3-5 + Batch 7 Slice C | TypeScript REST SDK package. `generated-wire-types.ts` is codegen-owned, `api-types.ts` is hand-maintained, and `wire-types.ts` preserves the public re-export path. Covers metrics, pricing visibility, audit search/redaction, backup verify, restore dry-run, read-only plan/advisory methods, inert workflow-run metadata methods, and read-only supervised patch workspace/artifact metadata methods. Batch 6 added no SDK API. | `AgentControlPlaneClient`, generated wire types, local API types | `bun run build`, `bun run test`, `npm pack --dry-run` |
| `sdk/python/` | Agent-Control-Plane Phase 5 + Local Small-Team Productization + Dashboard Controls + Production-like Local Ops Hardening + Supervised Autonomous Beta Planning Batch 3-5 + Batch 7 Slice C | Python REST SDK package. Covers metrics, pricing visibility, audit search/redaction, backup verify, restore dry-run, read-only plan/advisory methods, inert workflow-run metadata methods, and read-only supervised patch workspace/artifact metadata methods. Batch 6 added no SDK API. | `AgentControlPlaneClient`, generated wire types | `cd sdk/python && PYTHONPATH=src uv run --no-project python -m unittest discover -s tests` |
| `dashboard/` | Agent-Control-Plane Phase 6 + Local Small-Team Productization + Dashboard Controls + Post-R7 Wire/Type Governance + Dashboard UX Polish + Production-like Local Ops Hardening + Batch 7 Slice E | Next.js dashboard for live local dispatches, readable dispatch detail summaries, routing choices, team/API-key management, costs, operations metrics, pricing warnings, settings with provider health, health, backups, audit log, and supervised patch read-only metadata visibility. Dispatches and AuditLog use server-side pagination/search. Backups include verify and restore dry-run controls. SupervisedPatch component shows workspace/artifact metadata in a read-only tab with list/detail drill-down and boundary badges. ARIA-compliant tab navigation with keyboard support, modal focus traps, keyboard-accessible table rows, form labels, CSS spinner loading states, shared EmptyState/StateBanner/BoundaryBadges components, local setup checklist, permission-aware 401/403/config-required states backed by structured API error codes, actionable empty states, and consolidated visual utility classes. Wire-shaped enum fields reuse generated SDK union aliases through type-only imports. | App Router page, `fetchHealth`, `fetchDashboard`, `fetchDispatches`, `fetchDispatchDetail`, `fetchBackups`, `verifyBackup`, `restoreBackupDryRun`, `fetchAudit`, `fetchMetrics`, `fetchProviderHealth`, `fetchSupervisedPatchWorkspaces`, `fetchSupervisedPatchArtifacts`, readonly lint guard, `build:static` | `cd dashboard && npx tsc -p tsconfig.json --noEmit`, `node scripts/lint-readonly.mjs`, `npx next build`, `node scripts/build-static.mjs` |

## Codegen and Wire Contracts

| Module | Stage | Purpose | Main public APIs | Related tests |
| --- | --- | --- | --- | --- |
| `codegen/generate_wire_types.py` | Agent-Control-Plane Phase 5 + Post-R7 Wire/Type Governance | Deterministic wire-contract type generator for SDK surfaces. Practical enum values are read from JSON schemas; `--check` fails on checked-in output drift. | `main`, `render_ts`, `render_python`, `enum_values` | `uv run --no-project python codegen/generate_wire_types.py --check` |
| `wire_contract/v1/*.schema.json` | Language Migration Phase 0 + Post-R7 Wire/Type Governance | Governed dispatch JSON schemas for cross-language semantic parity. Execution-result enums include active CLI/provider variants. | `dispatch_request`, `task_analysis`, `dispatch_decision`, `execution_result`, `evaluation_result`, `dispatch_bundle` schemas | `cargo test -p engine --test dispatch_parity` |

## Utility Scripts

| Module | Stage | Purpose | Main public APIs | Related tests |
| --- | --- | --- | --- | --- |
| `scripts/acp_local_doctor.py` | Local setup readiness | Stdlib local setup checker for Node/npm/Bun/Cargo/uv, dashboard export, engine binary, port availability, and protected-mode auth shape. | `main`, `collect_checks` | `uv run --no-project python scripts/acp_local_doctor.py` |
| `scripts/bootstrap_local_auth.py` | Local setup readiness | Stdlib helper that generates a non-persisted local admin API key and prints protected-mode startup commands. | `main` | `uv run --no-project python scripts/bootstrap_local_auth.py --json` |
| `scripts/acp_ops_check.py` | Production-like local ops hardening | Stdlib operational checker for health, ready, metrics, storage integrity, provider health, backups, and costs against a local API. | `main`, `collect_checks` | `uv run --no-project python scripts/acp_ops_check.py --help`; temp protected engine smoke |
| `scripts/acp_restore_smoke.py` | Production-like local disaster-recovery smoke | Stdlib backup smoke that creates a local backup, verifies checksum/integrity, and runs restore dry-run by default. Real restore is optional and requires explicit flags. | `main`, `run_smoke` | `uv run --no-project python scripts/acp_restore_smoke.py --help`; temp protected engine smoke |
| `scripts/acp_secret_scan.py` | Production-like local security hardening | Stdlib secret scanner for git-tracked files plus local env files (`.env`, `.env.local`, `.env.production-like.local`) before local provider trials. | `main`, `scan_file` | `uv run --no-project python scripts/acp_secret_scan.py` |
| `scripts/start_production_like_local.sh` | Production-like local startup | Guarded local beta startup profile enforcing auth, provider execution gate, admin key shape, provider secret env indirection, dashboard export presence, SQLite path, and backup dir. | shell entrypoint | Requires filled `.env.production-like.local`; verified by inspection |
| `scripts/smoke_native_runtime.py` | Agent-Control-Plane Native Local Runtime | Stdlib smoke test for the Rust engine, static dashboard, dispatch/audit search, export, and structured backup auth boundary. | `main` | `uv run --no-project python scripts/smoke_native_runtime.py` |
| `scripts/trial4_real_use_pilot.py` | Trial 4 real-use pilot | Stdlib pilot runner that starts temporary local engines and exercises static dashboard, API key, noop dispatch, dispatch list/detail/search/pagination, audit, backup, export/import, provider-health, TypeScript SDK, Python SDK, and CLI-routing smoke flows without real provider calls. | `main` | `uv run --no-project python scripts/trial4_real_use_pilot.py` |
| `scripts/trial5_cli_execution_beta.py` | Trial 5 CLI execution beta | Stdlib pilot runner that starts temporary local engines and validates CLI default-off behavior, missing binary diagnostics, deterministic stub Codex/Claude routing, result fields, failure paths, audit/provider-audit behavior, cost/token safety, SDK visibility, and provider default-off boundaries without real provider calls. | `main` | `uv run --no-project python scripts/trial5_cli_execution_beta.py` |
| `scripts/verify_rust_typescript_stack.sh` | Rust + TypeScript Cutover | Primary cutover verification. | shell entrypoint | `bash scripts/verify_rust_typescript_stack.sh` |
| `scripts/check_agent_handoff.py` | Agent workflow | Handoff documentation integrity guard. Requires current R7 seal facts plus executable wire-codegen guard and verifier/CI wiring. | `main` | `uv run --no-project python scripts/check_agent_handoff.py` |
| `scripts/check_wire_codegen_drift.sh` | Post-R7 Wire/Type Governance | Fails when generated Rust/TypeScript/Python wire files differ from schema-driven codegen output. | shell entrypoint | `bash scripts/check_wire_codegen_drift.sh` |
| `tools/check_security_baseline.py` | CA-7 Security | Security baseline checker (secret scan, import scan, routing guard, governance guard, event guard). | `main`, `check_secret_scan`, `check_import_scan`, `check_active_routing`, `check_governance_boundary`, `check_stage0_event_guard` | `tools/test_security_baseline.py` |

## Infrastructure

| Module | Stage | Purpose | Main public APIs | Related tests |
| --- | --- | --- | --- | --- |
| `deploy/`, `docker-compose.yml` | Agent-Control-Plane Phase 7 | Optional local Docker build/run definitions for Rust API and dashboard. | `deploy/Dockerfile.engine`, `deploy/Dockerfile.dashboard`, compose services | `docker compose build`, `docker compose up --build -d` |
| `docs/AGENT_CONTROL_PLANE_MIGRATION_CLOSEOUT.md` | Agent-Control-Plane Phase 8 | Migration closeout record. | Closeout status | `uv run --no-project python scripts/check_agent_handoff.py` |
