{
  "schema_version": "benchmark_scenario.v1",
  "scenario_id": "code_fix_basic",
  "title": "Small code fix with explicit verification",
  "task_type": "code_fix",
  "objective": "Apply a minimal local code fix, run the smallest sufficient verification, and produce bounded evidence.",
  "success_criteria": [
    "changed files are minimal",
    "relevant verification passes",
    "failure output is summarized without raw secret-bearing logs",
    "target output remains approval-gated"
  ],
  "expected_context_pressure": "medium",
  "allowed_modes": [
    "native_control_plane",
    "stateless_reread",
    "stateful_store",
    "pruned_context"
  ],
  "quality_method": "test",
  "ci_provider_policy": "stub_only",
  "notes": "Use this scenario for early scorecard importer tests before more complex external runtime adapters."
}
