export const meta = {
  name: 'macro-orchestrator-phase3-queue-backpressure',
  description: 'Queue / Priority / Backpressure for workflow run scheduling. Adds priority, deadline, quota, queue position, and backpressure pause reason to runs.',
  phases: [
    { title: 'Wave 1: Storage + Queue Module', detail: 'Migration v9, queue metadata on runs, priority-aware run listing, RunQueue module, backpressure module, scheduler integration, DynamicController integration' },
    { title: 'Wave 2: HTTP + SDK + Dashboard', detail: 'API endpoints for queue status, SDK methods, Dashboard QueueStatus component' },
    { title: 'Wave 3: Verify', detail: 'cargo test, cargo fmt, cargo clippy, TypeScript build, handoff guard' },
  ],
}

// ============================================================
// Wave 1: Storage + Queue Module (Rust)
// ============================================================

// --- 1A: Migration v9 + Storage layer ---

const storageResult = await agent('You are implementing storage changes for Macro-Orchestrator Phase 3 (Queue / Priority / Backpressure).\n\n## Context\n- Project: token-efficient-agent-harness-lab (Rust engine at engine/)\n- Current schema version: 8 (in engine/src/storage/local_product_store/migrations.rs)\n- Current test count: 1243 Rust tests\n- The scheduler currently selects runs FIFO from list_active_workflow_run_ids() which queries:\n  WHERE status IN (\'running\', \'created\') ORDER BY run_sequence\n\n## Task: Migration v9 + Storage Methods\n\n### 1. Add migration v9 to engine/src/storage/local_product_store/migrations.rs\n\nAdd these columns to workflow_runs table:\n- priority INTEGER NOT NULL DEFAULT 5  (1=highest, 10=lowest, 5=default)\n- deadline_at TEXT  (optional ISO timestamp, NULL=no deadline)\n- sla_ms INTEGER  (optional milliseconds, NULL=no SLA)\n- tenant_id TEXT  (optional, NULL=unscoped)\n- queue_position INTEGER  (set when run enters queue, NULL when not queued)\n- pause_reason TEXT  (NULL=not paused, or: \'pool_saturated\', \'quota_exceeded\', \'deadline_passed\', \'operator_paused\')\n- degrade_mode TEXT  NULL=none, or: \'reduced_concurrency\', \'fallback_executor\', \'deferred\'\n\nAlso create index on workflow_runs(priority, created_at) for priority-aware queries.\n\nThe migration function should:\n- ALTER TABLE workflow_runs ADD COLUMN priority INTEGER NOT NULL DEFAULT 5\n- ALTER TABLE workflow_runs ADD COLUMN deadline_at TEXT\n- ALTER TABLE workflow_runs ADD COLUMN sla_ms INTEGER\n- ALTER TABLE workflow_runs ADD COLUMN tenant_id TEXT\n- ALTER TABLE workflow_runs ADD COLUMN queue_position INTEGER\n- ALTER TABLE workflow_runs ADD COLUMN pause_reason TEXT\n- ALTER TABLE workflow_runs ADD COLUMN degrade_mode TEXT\n- CREATE INDEX IF NOT EXISTS idx_workflow_runs_priority ON workflow_runs(priority, created_at)\n\nUpdate CURRENT_SCHEMA_VERSION to 9 and add the v9 entry to the match statement.\n\n### 2. Add storage methods to engine/src/storage/local_product_store/workflow_runs.rs\n\nAdd these new public methods to LocalProductStore:\n\na) list_active_workflow_runs_prioritized() -> Result<Vec<Value>>\n   Query: SELECT run_id, run_sequence, workflow_id, status, priority, deadline_at, sla_ms, tenant_id, queue_position, pause_reason, degrade_mode, created_at, started_at\n   FROM workflow_runs WHERE status IN (\'running\', \'created\')\n   ORDER BY CASE WHEN pause_reason IS NOT NULL THEN 1 ELSE 0 END, priority ASC, created_at ASC\n   Return rows as Vec<Value>.\n\nb) update_run_priority(run_id, priority) -> Result<()>\n   UPDATE workflow_runs SET priority = ?, updated_at = datetime(\'now\') WHERE run_id = ?\n\nc) update_run_pause_reason(run_id, pause_reason: Option<&str>) -> Result<()>\n   UPDATE workflow_runs SET pause_reason = ?, updated_at = datetime(\'now\') WHERE run_id = ?\n\nd) update_run_degrade_mode(run_id, degrade_mode: Option<&str>) -> Result<()>\n   UPDATE workflow_runs SET degrade_mode = ?, updated_at = datetime(\'now\') WHERE run_id = ?\n\ne) set_run_queue_position(run_id, position: Option<i32>) -> Result<()>\n   UPDATE workflow_runs SET queue_position = ?, updated_at = datetime(\'now\') WHERE run_id = ?\n\nf) get_queue_status() -> Result<Value>\n   Query aggregate stats:\n   - total_queued: COUNT(*) WHERE status IN (\'created\') AND pause_reason IS NULL\n   - total_running: COUNT(*) WHERE status = \'running\'\n   - total_paused: COUNT(*) WHERE pause_reason IS NOT NULL\n   - total_completed: COUNT(*) WHERE status = \'completed\'\n   - total_failed: COUNT(*) WHERE status = \'failed\'\n   - avg_priority: AVG(priority) WHERE status IN (\'created\', \'running\')\n   - overdue_count: COUNT(*) WHERE deadline_at IS NOT NULL AND deadline_at < datetime(\'now\') AND status IN (\'created\', \'running\')\n   Return as Value::Object with all fields.\n\ng) list_tenants_with_quota() -> Result<Vec<Value>>\n   Query: SELECT tenant_id, COUNT(*) as run_count, AVG(priority) as avg_priority\n   FROM workflow_runs WHERE status IN (\'created\', \'running\') AND tenant_id IS NOT NULL\n   GROUP BY tenant_id\n   Return as Vec<Value>.\n\n### 3. Update create_workflow_run_from_plan\n\nIn the existing create_workflow_run_from_plan method, ensure the INSERT includes the new columns with defaults (priority=5, deadline_at=NULL, sla_ms=NULL, tenant_id=NULL, queue_position=NULL, pause_reason=NULL, degrade_mode=NULL). The run_json blob should also include these fields.\n\nAlso add a separate method create_workflow_run_with_queue_metadata(plan_id, actor, priority, deadline_at, sla_ms, tenant_id) that passes the queue fields.\n\n### 4. Add tests\n\nAdd inline tests in workflow_runs.rs that cover:\n- list_active_workflow_runs_prioritized returns correct priority ordering\n- update_run_priority changes priority\n- update_run_pause_reason sets/clears pause reason\n- update_run_degrade_mode sets/clears degrade mode\n- set_run_queue_position sets/clears position\n- get_queue_status returns correct aggregate counts\n- list_tenants_with_quota groups correctly\n- create_workflow_run_with_queue_metadata stores all fields\n\nTarget: 10-15 new tests for storage layer.\n\n### Constraints\n- Follow existing code patterns exactly (see how other methods use conn.execute and conn.query_row)\n- No comments unless WHY is non-obvious\n- Use rusqlite::params![] macro for parameter binding\n- Run: cargo test -p engine --lib local_product_store\n\n### Files to modify\n- engine/src/storage/local_product_store/migrations.rs (add v9 migration)\n- engine/src/storage/local_product_store/mod.rs (update CURRENT_SCHEMA_VERSION to 9)\n- engine/src/storage/local_product_store/workflow_runs.rs (new methods + tests)',
  {label: 'storage:migration-v9', phase: 'Wave 1: Storage + Queue Module', model: 'opus'})

// --- 1B: RunQueue + Backpressure modules ---

const queueModule = await agent('You are creating two new Rust modules for Macro-Orchestrator Phase 3 (Queue / Priority / Backpressure).\n\n## Context\n- Project: token-efficient-agent-harness-lab (Rust engine at engine/)\n- These are brand new files, not modifying existing ones\n- The storage layer (being implemented separately) adds priority/deadline/tenant/pause_reason/degrade_mode columns to workflow_runs\n\n## Module 1: engine/src/workflow/run_queue.rs\n\nCreate a RunQueue module that provides priority-aware run ordering logic.\n\nThe module should define these structs:\n\nQueuedRun: { run_id: String, priority: i32, created_at: String, deadline_at: Option<String>, sla_ms: Option<i64>, tenant_id: Option<String>, pause_reason: Option<String>, queue_position: Option<i32> }\n\nQueueConfig: { max_concurrent: usize, max_queued: usize (default 100), priority_ceiling: i32 (default 10), priority_floor: i32 (default 1), deadline_overdue_threshold_ms: i64 (default 60000) }\n\nQueueStatus: { total_queued: usize, total_running: usize, total_paused: usize, total_completed: u64, total_failed: u64, avg_priority: f64, overdue_count: usize, capacity_utilization: f64, queue_depth_ratio: f64, backpressure_active: bool, tenant_counts: Vec<TenantQueueInfo> }\n\nTenantQueueInfo: { tenant_id: String, run_count: usize, avg_priority: f64 }\n\nAdmissionResult: { allowed: bool, reason: String, suggested_action: Option<String> }\n\nDeadlineAction: { run_id: String, action: String, reason: String }\n\nBackpressureSignal: { active: bool, degrade_mode: Option<String>, pause_targets: Vec<String>, priority_ceiling_temp: Option<i32>, reason: String }\n\nRunQueue struct with methods:\n- new(config) -> Self\n- sort_runs(runs: Vec<QueuedRun>) -> Vec<QueuedRun>: sort by non-paused first, then priority ASC (lower=higher), then deadline proximity, then created_at FIFO\n- admission_check(queue_status: &QueueStatus) -> AdmissionResult: deny if total_queued >= max_queued, warn if utilization > 0.9\n- check_deadlines(runs: &[QueuedRun]) -> Vec<DeadlineAction>: find overdue runs, suggest boost for approaching deadlines\n- compute_backpressure(queue_status: &QueueStatus) -> BackpressureSignal: activate if utilization > 80% or queue_depth > 80%\n- validate_priority(priority: i32) -> Result<i32, String>: clamp to [floor, ceiling]\n\nInclude 12-16 inline tests.\n\n## Module 2: engine/src/workflow/backpressure.rs\n\nCreate a Backpressure module that manages the backpressure lifecycle.\n\nBackpressureConfig: { enabled: bool (default true), activation_threshold: f64 (default 0.8), deactivation_threshold: f64 (default 0.5), max_paused_runs: usize (default 10), degrade_concurrency_factor: f64 (default 0.5), cooldown_after_pause_ms: u64 (default 60000) }\n\nPauseAction: { run_id: String, reason: String, paused_at: String }\n\nBackpressureDecision: { active: bool, action: String, runs_to_pause: Vec<String>, degrade_mode: Option<String>, effective_concurrency: usize, reason: String }\n\nBackpressure struct with methods:\n- new(config) -> Self\n- evaluate(pool_utilization, queue_depth, max_queue, overdue_count, current_timestamp_ms) -> BackpressureDecision\n- should_skip_run(run_id, current_timestamp_ms) -> bool\n- record_pause(run_id, timestamp_ms)\n- effective_max_concurrent(base: usize, active: bool) -> usize\n- cleanup_expired_cooldowns(current_timestamp_ms)\n\nInclude 10-14 inline tests.\n\n## Module 3: engine/src/workflow/mod.rs\n\nAdd to the workflow module declarations:\npub mod run_queue;\npub mod backpressure;\n\nAlso re-export key types from both modules.\n\n## Constraints\n- No comments unless WHY is non-obvious\n- Follow existing code patterns in engine/src/workflow/\n- All methods must be deterministic\n- Tests use #[cfg(test)] mod tests { ... } inline\n- Run: cargo test -p engine --lib workflow::run_queue && cargo test -p engine --lib workflow::backpressure\n- Run: cargo fmt && cargo clippy -p engine --all-targets -- -D warnings\n\n### Files to create\n- engine/src/workflow/run_queue.rs\n- engine/src/workflow/backpressure.rs\n### Files to modify\n- engine/src/workflow/mod.rs',
  {label: 'modules:run-queue-backpressure', phase: 'Wave 1: Storage + Queue Module', model: 'opus'})

// --- 1C: Scheduler + DynamicController integration ---

const schedulerResult = await agent('You are integrating Queue/Priority/Backpressure into the scheduler and DynamicWorkflowController for Macro-Orchestrator Phase 3.\n\n## Context\n- Project: token-efficient-agent-harness-lab (Rust engine at engine/)\n- Two new modules are being created in parallel: engine/src/workflow/run_queue.rs and engine/src/workflow/backpressure.rs\n- Storage layer adds: priority, deadline_at, sla_ms, tenant_id, queue_position, pause_reason, degrade_mode columns to workflow_runs, and list_active_workflow_runs_prioritized() method\n- Current test count: 1243 Rust tests\n\n## Task 1: Modify engine/src/scheduler.rs\n\n### Changes to SchedulerConfig (add fields):\n- queue_enabled: bool (env ACP_QUEUE_ENABLED, default true)\n- max_queued: usize (env ACP_MAX_QUEUED, default 100)\n- backpressure_enabled: bool (env ACP_BACKPRESSURE_ENABLED, default true)\n- backpressure_activation: f64 (env ACP_BACKPRESSURE_ACTIVATION, default 0.8)\n\nAdd these to from_env() with appropriate env var parsing.\n\n### Changes to TickResult (add fields):\n- paused_runs: Vec<String>\n- degraded_runs: Vec<String>\n- backpressure_active: bool\n- queue_depth: usize\n\n### Changes to scheduler_tick function:\n\nReplace the FIFO run selection with priority-aware selection:\n\nWhen queue_enabled is true, use store.list_active_workflow_runs_prioritized() instead of list_active_workflow_run_ids(). Filter out runs where pause_reason is not NULL. Then apply backpressure if enabled: compute pool utilization as total_active/total_capacity, create a Backpressure instance, call evaluate(), and for each run_to_pause call store.update_run_pause_reason() and store.update_run_degrade_mode().\n\nWhen queue_enabled is false, keep existing FIFO behavior.\n\n### Changes to dynamic_scheduler_tick:\n\nApply the same priority-aware selection in dynamic mode.\n\n### Add queue info to scheduler status:\n\nThe scheduler status should include: queue_enabled, backpressure_active, queue_depth, paused_runs_count.\n\n## Task 2: Modify engine/src/workflow/dynamic_controller.rs\n\n### Changes to DynamicControllerConfig (add fields):\n- admission_check_enabled: bool (default true)\n- respect_priority: bool (default true)\n\n### Changes to ControllerTickResult (add fields):\n- queue_position: Option<i32>\n- priority: Option<i32>\n- admission_allowed: bool\n- admission_reason: Option<String>\n\n### Changes to DynamicWorkflowController::tick:\n\nAt the start of tick(), before Phase 1 (auto-fix), add an admission check: read the run data from store.get_workflow_run(run_id), and if pause_reason is non-empty, return a NoAction result with admission_allowed=false and the pause reason.\n\nAlso populate priority and queue_position from the run data in the returned ControllerTickResult.\n\n## Task 3: Add tests\n\nIn engine/src/scheduler.rs (or extend existing test section):\n- scheduler_tick with queue_enabled uses prioritized ordering\n- scheduler_tick with backpressure pauses overloaded runs\n- dynamic_scheduler_tick respects paused runs\n\nIn engine/src/workflow/dynamic_controller.rs:\n- tick respects pause_reason (returns NoAction)\n- tick includes priority and queue_position in result\n\nTarget: 8-12 new tests.\n\n## Constraints\n- Maintain backward compatibility: queue_enabled=false should produce identical behavior to current FIFO\n- No comments unless WHY is non-obvious\n- Follow existing patterns exactly\n- Run: cargo test -p engine --lib scheduler && cargo test -p engine --lib workflow::dynamic_controller\n- Run: cargo fmt && cargo clippy -p engine --all-targets -- -D warnings\n\n### Files to modify\n- engine/src/scheduler.rs\n- engine/src/workflow/dynamic_controller.rs',
  {label: 'scheduler:queue-integration', phase: 'Wave 1: Storage + Queue Module', model: 'opus'})

// ============================================================
// Wave 2: HTTP + SDK + Dashboard (after Wave 1)
// ============================================================

const apiResult = await agent('You are adding HTTP API endpoints for Queue / Priority / Backpressure for Macro-Orchestrator Phase 3.\n\n## Context\n- Project: token-efficient-agent-harness-lab (Rust engine at engine/)\n- Storage layer has: get_queue_status(), list_active_workflow_runs_prioritized(), update_run_priority(), update_run_pause_reason(), list_tenants_with_quota()\n- HTTP server uses axum at engine/src/http_server/\n\n## Task 1: Create engine/src/http_server/handlers/queue.rs\n\nEndpoints:\n\nGET /api/v1/queue/status - Returns full queue status from store.get_queue_status() plus backpressure_active, effective_concurrency, queue_config.\n\nGET /api/v1/queue/runs - Returns prioritized list of active runs with queue metadata. Query params: limit (default 50), offset (default 0).\n\nPUT /api/v1/queue/runs/{run_id}/priority - Request body: { priority: i32 }. Validates [1,10], updates via store.update_run_priority().\n\nPUT /api/v1/queue/runs/{run_id}/pause - Request body: { reason: Option<String> }. Sets/clears pause_reason.\n\nGET /api/v1/queue/tenants - Returns tenant quota info.\n\n## Task 2: Register routes\n\nIn engine/src/http_server/routes.rs, add queue routes under /api/v1/queue/.\n\nAlso register in handlers/mod.rs.\n\n## Task 3: Add tests in engine/tests/test_http_server.rs\n\n- GET /api/v1/queue/status returns 200\n- GET /api/v1/queue/runs returns array\n- PUT /api/v1/queue/runs/{run_id}/priority updates priority\n- PUT /api/v1/queue/runs/{run_id}/priority returns 400 for invalid\n- PUT /api/v1/queue/runs/{run_id}/pause sets/clears\n- GET /api/v1/queue/tenants returns array\n\nTarget: 7-10 new tests.\n\n## Constraints\n- Follow existing handler patterns exactly\n- No comments unless WHY is non-obvious\n- Run: cargo test -p engine --test test_http_server\n- Run: cargo fmt && cargo clippy -p engine --all-targets -- -D warnings\n\n### Files to create\n- engine/src/http_server/handlers/queue.rs\n### Files to modify\n- engine/src/http_server/handlers/mod.rs\n- engine/src/http_server/routes.rs\n- engine/tests/test_http_server.rs',
  {label: 'http:queue-endpoints', phase: 'Wave 2: HTTP + SDK + Dashboard', model: 'opus'})

const sdkDashboardResult = await agent('You are adding SDK methods and a Dashboard component for Queue / Priority / Backpressure for Macro-Orchestrator Phase 3.\n\n## Context\n- Project: token-efficient-agent-harness-lab\n- New HTTP endpoints: GET /api/v1/queue/status, GET /api/v1/queue/runs, PUT /api/v1/queue/runs/{run_id}/priority, PUT /api/v1/queue/runs/{run_id}/pause, GET /api/v1/queue/tenants\n- TypeScript SDK at sdk/typescript/\n- Python SDK at sdk/python/\n- Dashboard at dashboard/\n\n## Task 1: TypeScript SDK (sdk/typescript/)\n\nAdd to sdk/typescript/src/api-types.ts:\n- QueueStatus interface (total_queued, total_running, total_paused, total_completed, total_failed, avg_priority, overdue_count, capacity_utilization, queue_depth_ratio, backpressure_active, effective_concurrency, queue_config, tenant_counts)\n- QueueConfig interface (max_concurrent, max_queued, backpressure_enabled, backpressure_activation)\n- TenantQueueInfo interface (tenant_id, run_count, avg_priority)\n- QueueRunSummary interface (run_id, run_sequence, workflow_id, status, priority, deadline_at, sla_ms, tenant_id, queue_position, pause_reason, degrade_mode, created_at, started_at)\n- UpdatePriorityRequest interface (priority: number)\n- PauseRunRequest interface (reason: string | null)\n\nAdd to sdk/typescript/src/index.ts (AgentControlPlaneClient class):\n- fetchQueueStatus(): Promise<QueueStatus>\n- fetchQueueRuns(limit?, offset?): Promise<QueueRunSummary[]>\n- updateRunPriority(runId, priority): Promise<QueueRunSummary>\n- pauseRun(runId, reason): Promise<QueueRunSummary>\n- fetchQueueTenants(): Promise<TenantQueueInfo[]>\n\n## Task 2: Python SDK (sdk/python/)\n\nAdd to sdk/python/src/agent_control_plane_client.py:\n- fetch_queue_status() -> dict\n- fetch_queue_runs(limit=50, offset=0) -> list\n- update_run_priority(run_id, priority) -> dict\n- pause_run(run_id, reason=None) -> dict\n- fetch_queue_tenants() -> list\n\n## Task 3: Dashboard component\n\nCreate dashboard/src/components/QueueStatus.tsx:\n1. Queue overview cards: Total Queued, Running, Paused, Overdue\n2. Capacity bar showing capacity_utilization\n3. Backpressure indicator badge\n4. Priority distribution table\n5. Tenant breakdown table\n6. Paused runs list with pause_reason and degrade_mode\n\nAdd a "Queue" tab to the dashboard navigation.\n\n## Task 4: Build verification\n\nRun:\n- cd sdk/typescript && bun run build\n- cd dashboard && bun run build:static\n\nFix any TypeScript errors.\n\n## Constraints\n- Follow existing SDK and dashboard patterns exactly\n- No comments unless WHY is non-obvious\n- All new TypeScript interfaces in api-types.ts, NOT in generated-wire-types.ts\n\n### Files to modify\n- sdk/typescript/src/api-types.ts\n- sdk/typescript/src/index.ts\n- sdk/python/src/agent_control_plane_client.py\n- dashboard/src/components/QueueStatus.tsx (new)\n- dashboard/src/app/page.tsx or equivalent',
  {label: 'sdk-dashboard:queue', phase: 'Wave 2: HTTP + SDK + Dashboard', model: 'opus'})

// ============================================================
// Wave 3: Verify
// ============================================================

const verifyResult = await agent('You are verifying the Macro-Orchestrator Phase 3 (Queue / Priority / Backpressure) implementation.\n\n## Verification Steps\n\nRun ALL of these commands and report results:\n\n1. cargo test -p engine (full Rust test suite)\n2. cargo fmt --check\n3. cargo clippy -p engine --all-targets -- -D warnings\n4. cd sdk/typescript && bun run build\n5. cd dashboard && bun run build:static\n6. uv run --no-project python scripts/check_agent_handoff.py\n7. bash scripts/check_wire_codegen_drift.sh\n\nReport:\n- Test count before and after\n- Any failures with full error output\n- Any clippy warnings\n- Any TypeScript build errors\n- Any handoff guard failures\n\nIf any step fails, provide the exact error and suggest the fix.\n\n### Constraints\n- Do NOT modify any files - read-only verification pass\n- Report all findings faithfully',
  {label: 'verify:phase3-full', phase: 'Wave 3: Verify', model: 'sonnet'})

// ============================================================
// Summary
// ============================================================

log('Phase 3 implementation workflow complete.')
log('Storage + Migration: ' + (storageResult ? 'DONE' : 'SKIPPED'))
log('RunQueue + Backpressure modules: ' + (queueModule ? 'DONE' : 'SKIPPED'))
log('Scheduler integration: ' + (schedulerResult ? 'DONE' : 'SKIPPED'))
log('HTTP API endpoints: ' + (apiResult ? 'DONE' : 'SKIPPED'))
log('SDK + Dashboard: ' + (sdkDashboardResult ? 'DONE' : 'SKIPPED'))
log('Verification: ' + (verifyResult || 'pending'))
