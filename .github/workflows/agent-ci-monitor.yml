name: agent-ci-monitor

on:
  workflow_run:
    workflows: [tests]
    types: [completed]

permissions:
  actions: write
  contents: read
  issues: write
  pull-requests: read

concurrency:
  group: agent-ci-monitor-${{ github.event.workflow_run.id }}
  cancel-in-progress: false

jobs:
  process:
    runs-on: ubuntu-latest
    env:
      AGENT_REPO: ${{ github.repository }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: main
          persist-credentials: false
      - uses: actions/setup-python@8d9ed9ac5c53483de85588cdf95a591a75ab9f55 # v5
        with:
          python-version: "3.11"
      - name: Exit without mutation when controls are disabled or emergency-stopped
        id: gate
        run: |
          if python3 scripts/agent-control/control_state.py require-live >/dev/null; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Process exact canonical workflow_run evidence
        id: decision
        if: steps.gate.outputs.enabled == 'true'
        run: |
          set -euo pipefail
          python3 scripts/agent-control/control_state.py require-live >/dev/null
          python3 scripts/agent-control/ci_handler.py > "$RUNNER_TEMP/ci-decision.json"
          cat "$RUNNER_TEMP/ci-decision.json"
          echo "action=$(jq -er '.action' "$RUNNER_TEMP/ci-decision.json")" >> "$GITHUB_OUTPUT"
          for key in pr_number issue_number head_sha ci_run_id repair_count; do
            value=$(jq -r --arg key "$key" '.[$key] // empty' "$RUNNER_TEMP/ci-decision.json")
            echo "$key=$value" >> "$GITHUB_OUTPUT"
          done
      - name: Dispatch bounded CI repair by exact failed run ID
        if: steps.decision.outputs.action == 'trigger_repair'
        run: |
          python3 scripts/agent-control/control_state.py require-live >/dev/null
          gh workflow run agent-controller.yml --ref main \
            -f command=dispatch-repair \
            -f pr_number="${{ steps.decision.outputs.pr_number }}" \
            -f issue="${{ steps.decision.outputs.issue_number }}" \
            -f head_sha="${{ steps.decision.outputs.head_sha }}" \
            -f ci_run_id="${{ steps.decision.outputs.ci_run_id }}" \
            -f repair_count="${{ steps.decision.outputs.repair_count }}"
      - name: Dispatch independent exact-head review
        if: steps.decision.outputs.action == 'trigger_review'
        run: |
          python3 scripts/agent-control/control_state.py require-live >/dev/null
          gh workflow run agent-controller.yml --ref main \
            -f command=dispatch-review \
            -f pr_number="${{ steps.decision.outputs.pr_number }}" \
            -f issue="${{ steps.decision.outputs.issue_number }}" \
            -f head_sha="${{ steps.decision.outputs.head_sha }}"
      - name: Dispatch merge only through the auto-merge control gate
        if: steps.decision.outputs.action == 'merge_ready'
        run: |
          if python3 scripts/agent-control/control_state.py require-auto-merge >/dev/null; then
            gh workflow run agent-controller.yml --ref main \
              -f command=dispatch-merge \
              -f pr_number="${{ steps.decision.outputs.pr_number }}" \
              -f issue="${{ steps.decision.outputs.issue_number }}" \
              -f head_sha="${{ steps.decision.outputs.head_sha }}"
          fi
      - name: Release the exact-head CI failure's active capacity
        if: steps.decision.outputs.action == 'blocked'
        run: |
          python3 scripts/agent-control/state_manager.py release-failed \
            "${{ steps.decision.outputs.issue_number }}" any agent-blocked \
            "${{ steps.decision.outputs.head_sha }}"
